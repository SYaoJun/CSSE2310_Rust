# Details for test 8.1.
# $1 will be run_test, explain_test or mark_test.
function test8.1() {
    "$1" 8.1 "evaluate_single_stdin01" \
        "Check uqbasejump operates correctly when the newline character is entered via stdin to evaluate an expression consisting of a single number with a small number consisting of a single character" \
        --timeout 5 \
        --maxattempts 1 \
        --stdin "testfiles/8.1.in" \
        --exit 0 \
        --stdout testfiles/8.1.stdout \
        --stderr testfiles/empty \
        "./uqbasejump" 
}
