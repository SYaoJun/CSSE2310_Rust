# Details for test 5.1.
# $1 will be run_test, explain_test or mark_test.
function test5.1() {
    "$1" 5.1 "check_reap_child_processes01" \
        "Check uqcshell correctly implements checking and reaping child processes after each line is read from stdin with echo and two arguments" \
        --timeout 10 \
        --maxattempts 2 \
        --stdin "testfiles/5.1.commands" \
        --exit 15 \
        --stdout testfiles/5.1.stdout \
        --stderr testfiles/empty \
        "testfiles/run_uqcshell.sh" '0.1' 'exitTerm'
}
