# Details for test 5.2.
# $1 will be run_test, explain_test or mark_test.
function test5.2() {
    "$1" 5.2 "check_reap_child_processes02" \
        "Check uqcshell correctly implements checking and reaping child processes after each line is read from stdin with cat with no arguments" \
        --timeout 10 \
        --maxattempts 2 \
        --stdin "testfiles/5.2.commands" \
        --exit 15 \
        --stdout testfiles/5.2.stdout \
        --stderr testfiles/empty \
        "testfiles/run_uqcshell.sh" '0.1' 'exitTerm'
}
