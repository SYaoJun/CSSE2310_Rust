# Details for test 9.7.
# $1 will be run_test, explain_test or mark_test.
function test9.7() {
    "$1" 9.7 "evaluate_multi_stdin07" \
        "Check uqbasejump operates correctly when the newline character is entered via stdin to evaluate expressions consisting of multiple numbers and arithmetic operations with invalid operations" \
        --timeout 5 \
        --maxattempts 1 \
        --stdin "testfiles/9.7.in" \
        --exit 0 \
        --stdout testfiles/9.7.stdout \
        --stderr testfiles/9.7.stderr \
        "./uqbasejump" 
}
