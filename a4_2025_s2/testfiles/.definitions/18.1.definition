# Details for test 18.1.
# $1 will be run_test, explain_test or mark_test.
function test18.1() {
    "$1" 18.1 "server_stats_port_listen01" \
        "(CSSE7231) Check ratsserver handles inability to listen on stats port after listening on primary port" \
        --timeout 15 \
        --maxattempts 5 \
        --optional \
        --stdin "/dev/null" \
        --exit 4 \
        --stdout testfiles/18.1.stdout \
        --stderr testfiles/18.1.stderr \
        "testfiles/check_server_stats_port.sh" '--bad'
}
