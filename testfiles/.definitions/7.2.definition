# Details for test 7.2.
# $1 will be run_test, explain_test or mark_test.
function test7.2() {
    "$1" 7.2 "sigint_forwarding02" \
        "Check uqcshell correctly implements handles SIGINT (Ctrl-C) and SIGINT forwarding with a terminated echo" \
        --timeout 10 \
        --maxattempts 2 \
        --stdin "testfiles/7.2.commands" \
        --exit 15 \
        --stdout testfiles/7.2.stdout \
        --stderr testfiles/7.2.stderr \
        "testfiles/run_uqcshell.sh" '0.1' 'exitTerm'
}
