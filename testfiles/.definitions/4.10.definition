# Details for test 4.10.
# $1 will be run_test, explain_test or mark_test.
function test4.10() {
    "$1" 4.10 "client_user_and_server_input10" \
        "Check ratsclient handles a card that the user has already played" \
        --timeout 15 \
        --maxattempts 5 \
        --stdin "/dev/null" \
        --stdout testfiles/4.10.stdout \
        "testfiles/check_client_sequence.sh" 'testfiles/4.10.sequence' 'Harry' 'game'
}
